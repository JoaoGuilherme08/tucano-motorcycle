[phases.setup]
nixPkgs = ["nodejs_20", "python311", "gcc", "gnumake"]

[phases.install]
cmds = ["cd backend && npm install && npm rebuild better-sqlite3"]

[start]
cmd = "cd backend && node server.js"

